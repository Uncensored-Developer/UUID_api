UUID API
========

Steps to get running
--------------------
* Install project dependencies
    $ pip install -r requirements.txt
* run app
    $ python app.py
    
 
 ROUTES
 ------
 
 GET  localhost:5001/

